<div class="panel panel-primary">
  <div class="panel-heading">
    <h2 class="panel-title">Welcome <%= @current_user.username %></h2>
  </div>
  <div class="panel-body">
    <div>
      <h3>your muthafuckin surveys:</h3>
        <div>
          <ul> <% @user_surveys.each do |survey| %>
            <li> <a href="/surveys/<%=survey.id%>"><%= survey.title %></a> </li>
          </ul> <% end %>
        </div>
    </div>
      <a href="/surveys/new"><button type="button" class="btn btn-default" aria-label="Left Align">
      <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> New Survey
      </button></a>
  </div>
</div>
